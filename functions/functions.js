function addSum(num1, num2) {
    console.log(num1 + num2);
}

addSum(5,7);
addSum(10,12);

// Returning something from a function

function square(num) {
    return num*num;
}

addOne = square(3) + 1
console.log(addOne);

