// for of loops
// for of loops are used to iterate over iterable objects (arrays, strings, etc)

const months = ['January', 'February', 'March', 'April', 'May', 'June', 'July'];

for (const month of months) {
    console.log(month);
} 