const color = {
    red: '#FF0000',
    green: '#00FF00',
    blue: '#0000FF'
};

for (let c in color)
    console.log(c, color[c]);


let fruits = ['apple', 'banana', 'orange', 'mango'];

for (let f in fruits)
    console.log(f, fruits[f]);